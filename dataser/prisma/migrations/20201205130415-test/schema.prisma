datasource db {
  provider = "sqlite" 
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Record {
  id          Int      @id @default(autoincrement())
  lineId      Int
  statusCd    String
  message     String
  testcol     String?
  deletedAt   String?
  createdAt   DateTime @default(now())
}
