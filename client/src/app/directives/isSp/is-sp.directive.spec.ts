import { IsSpDirective } from './is-sp.directive';

describe('IsSpDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSpDirective();
    expect(directive).toBeTruthy();
  });
});
