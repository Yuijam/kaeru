import { IfDeviceDirective } from './if-device.directive';

describe('IfDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new IfDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
